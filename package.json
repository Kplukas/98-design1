{
  "name": "98-kitas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --port=4098 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KPlukas",
  "license": "MIT",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
